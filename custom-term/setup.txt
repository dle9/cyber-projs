*https://www.msys2.org/docs/terminals/

# install mysys2
https://www.msys2.org/

# upgrade pkgs 
pacman -Syu

# install packages
pacman -S git vim p7zip 

# upgrade rust
rustup upgrade

# edit settings.json 
(right click terminal > settings > open json)
follow msys2 docs to add to your terminal

# customization
see custom-term folder

# other installs
exiftools.exe > ~/tools/bin

# optimize
$ mkpasswd -l -c > /etc/passwd
$ mkgroup -l -c > /etc/group
- comment out db in /etc/nsswitch.conf
